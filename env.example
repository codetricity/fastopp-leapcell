# FastOpp PostgreSQL Edition - Local Development Environment
# Copy this file to .env and adjust the values for your local setup

# Database Configuration
# For local PostgreSQL (adjust username and database name as needed)
DATABASE_URL=postgresql+psycopg2://username@localhost:5432/fastopp_test

# For SQLite (alternative for development)
# DATABASE_URL=sqlite+aiosqlite:///./test.db

# Security
# Generate a secure secret key with: uv run python oppman.py secrets
SECRET_KEY=your_secure_secret_key_here

# Environment
ENVIRONMENT=development

# File Uploads
# Local development - files stored in static/uploads/
UPLOAD_DIR=static/uploads

# For testing with /tmp/ (like LeapCell)
# UPLOAD_DIR=/tmp/uploads

# AI Features (Optional)
# Get your API key from https://openrouter.ai/
OPENROUTER_API_KEY=your_openrouter_api_key_here

# LeapCell Object Storage (Optional - for file persistence)
# Only needed if you want to test Object Storage features locally
S3_ACCESS_KEY=your_leapcell_s3_access_key
S3_SECRET_KEY=your_leapcell_s3_secret_key
S3_BUCKET=your_leapcell_s3_bucket_name
S3_ENDPOINT_URL=https://objstorage.leapcell.io
S3_REGION=us-east-1
